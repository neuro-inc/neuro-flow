name: CI

on:
  push:
    branches: [ master ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ master ]


jobs:
  explore:
    runs-on: ubuntu-latest
    name: Name
    steps:
    - env:
      run: |
        echo ${{ job.name }} ${{ job.status }}
